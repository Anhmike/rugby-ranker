package com.ricknout.rugbyranker.matches.vo.detail.timeline

enum class TimelineEventType {
    TRY,
    CONVERSION,
    PENALTY,
    PENALTY_TRY,
    MISSED_CONVERSION,
    MISSED_PENALTY,
    SUB_ON,
    SUB_OFF,
    YELLOW_CARD,
    RED_CARD
}
